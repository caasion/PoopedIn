# ── Database ─────────────────────────────────────────────────────────────────
# Local dev: file:./dev.db
# Railway:   file:/data/dev.db  (volume must be mounted at /data)
DATABASE_URL="file:./dev.db"

# ── AI Poop Verification ──────────────────────────────────────────────────────
# Get your key at https://app.roboflow.com → Settings → Roboflow API
ROBOFLOW_API_KEY=

# ── Cloudflare R2 Image Storage ───────────────────────────────────────────────
# Account ID: Cloudflare Dashboard → right sidebar
R2_ACCOUNT_ID=

# R2 API Token credentials (Dashboard → R2 → Manage R2 API Tokens → Create Token)
# Permissions: Object Read & Write on your bucket
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

# Bucket name (exactly as created in R2)
R2_BUCKET_NAME=poopedin-uploads

# Public URL for your bucket (Dashboard → R2 → your bucket → Settings → Public Access)
# e.g. https://pub-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.r2.dev
R2_PUBLIC_URL=
